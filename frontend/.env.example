# Public address of the Go Backend. Never put provider API keys here.
VITE_API_BASE_URL=http://127.0.0.1:8080
# Optional same-origin proxy, used when VITE_API_BASE_URL is explicitly empty.
API_PROXY_TARGET=http://127.0.0.1:8080
